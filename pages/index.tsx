import type {NextPage} from 'next'
import Head from 'next/head'
import {FeedLayout, LeftSidebar, Navbar, RightSidebar} from "../layouts";

const Home: NextPage = () => {
    return (
        <div className={'h-[110vh] overflow-y-scroll scrollbar-hide'}>x
            <Head>
                <title>Facebook</title>
                <meta name="description" content="Generated by create next app"/>
                <link rel="icon" href="/favicon.ico"/>
            </Head>

            <Navbar/>
            <main className={'h-full w-full max-w-[1536px] mx-auto flex pt-20 px-1.5 sm:space-x-3'}>
                <LeftSidebar/>
                <FeedLayout/>
                <RightSidebar/>
            </main>
        </div>
    )
}

export default Home
