import type {NextPage} from 'next'
import Head from 'next/head'
import {Feed, LeftSidebar, Navbar, RightSidebar} from "../layouts";
// import {PencilAltIcon} from "@heroicons/react/outline";

const Home: NextPage = () => {
    return (
        <div className={'h-screen'}>
            <Head>
                <title>Facebook</title>
                <meta name="description" content="Generated by create next app"/>
                <link rel="icon" href="/favicon.ico"/>
            </Head>

            <Navbar/>
            <main className={'h-full w-full max-w-[1536px] mx-auto flex pt-20 px-1.5 sm:space-x-3 bg-sky-200/0'}>
                <LeftSidebar/>
                <Feed/>
                <RightSidebar/>
            </main>
        </div>
    )
}

export default Home
